{
  "name": "graphsentry",
  "displayName": "GraphSentry",
  "description": "Local-first, multi-language code graph explorer. Ask architecture questions, see impact analysis and call-flow diagrams, right inside VS Code.",
  "version": "0.3.0",
  "publisher": "cloudct",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloud-ct/graphsentry.git",
    "directory": "editors/vscode"
  },
  "bugs": {
    "url": "https://github.com/cloud-ct/graphsentry/issues"
  },
  "homepage": "https://github.com/cloud-ct/graphsentry#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization",
    "Machine Learning"
  ],
  "keywords": [
    "code graph",
    "architecture",
    "impact analysis",
    "coupling",
    "mermaid",
    "BYOK"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "graphsentry.analyze",
        "title": "GraphSentry: Analyze Workspace",
        "icon": "$(graph)"
      },
      {
        "command": "graphsentry.coupling",
        "title": "GraphSentry: Show Most Coupled Symbols"
      },
      {
        "command": "graphsentry.impact",
        "title": "GraphSentry: Impact Analysis for Symbol at Cursor"
      },
      {
        "command": "graphsentry.dependencies",
        "title": "GraphSentry: Dependencies of Symbol at Cursor"
      },
      {
        "command": "graphsentry.configureProvider",
        "title": "GraphSentry: Configure LLM Provider (API Key for Ask)"
      },
      {
        "command": "graphsentry.clearProvider",
        "title": "GraphSentry: Clear LLM Provider Configuration"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "graphsentry.impact",
          "group": "graphsentry",
          "when": "editorHasSelection == false"
        },
        {
          "command": "graphsentry.dependencies",
          "group": "graphsentry",
          "when": "editorHasSelection == false"
        }
      ],
      "commandPalette": [
        { "command": "graphsentry.analyze" },
        { "command": "graphsentry.coupling" },
        { "command": "graphsentry.impact" },
        { "command": "graphsentry.dependencies" },
        { "command": "graphsentry.configureProvider" },
        { "command": "graphsentry.clearProvider" },
        { "command": "graphsentry.flow", "when": "false" },
        { "command": "graphsentry.ask", "when": "false" }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "graphsentry",
          "title": "GraphSentry",
          "icon": "images/activitybar-icon.svg"
        }
      ]
    },
    "views": {
      "graphsentry": [
        {
          "id": "graphsentry.commands",
          "name": "Commands"
        }
      ]
    },
    "configuration": {
      "title": "GraphSentry",
      "properties": {
        "graphsentry.binaryPath": {
          "type": "string",
          "default": "",
          "description": "Path to a graphsentry binary to use instead of the extension's auto-downloaded copy. Leave empty to let the extension manage it."
        },
        "graphsentry.codeLens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show inline dependents/dependencies counts and a 'Show flow' link above analyzed symbols."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && npm run copy-assets",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "copy-assets": "node ./scripts/copy-assets.js",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "marked": "^12.0.2",
    "mermaid": "^10.9.1",
    "svg-pan-zoom": "^3.6.2",
    "typescript": "^5.4.0"
  }
}
